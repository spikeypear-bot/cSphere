package com.example.connect_sphere.venue.entity;

/** Selectable catalogue layouts; lowercase values are also the API/DB labels. */
public enum VenueLayout {
    classroom,
    theatre,
    boardroom,
    banquet,
    exhibition
}
