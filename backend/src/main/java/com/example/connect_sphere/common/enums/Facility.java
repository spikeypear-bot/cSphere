package com.example.connect_sphere.common.enums;

/** Labels match the existing PostgreSQL facilities enum exactly. */
public enum Facility {
    audio_visual_equipment,
    air_conditioning,
    breakout_spaces,
    projection,
    stage,
    dining_area,
    barbeque_pit
}
