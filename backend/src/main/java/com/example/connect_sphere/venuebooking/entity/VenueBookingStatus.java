package com.example.connect_sphere.venuebooking.entity;

/** Existing PostgreSQL labels; VS16 never transitions status. */
public enum VenueBookingStatus {
    pending, confirmed, changed, rejected, cancelled
}
