# ConnectSphere — see AI_Context.md

This project's context, rules, and conventions live in **`AI_Context.md`** at the repository root — that file (not this one) is the single source of truth, kept tool-agnostic since not everyone on the team uses Claude.

This file exists only because Claude Code specifically auto-loads a file named `CLAUDE.md` as project context at the start of every session. Its only job is to point here. Do not duplicate content into this file — edit `AI_Context.md` instead, and it stays in sync for every tool automatically.
